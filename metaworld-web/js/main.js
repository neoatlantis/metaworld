require([
    'ui.filter',
    'ui.browser',
    'dbmanage',
    'jquery',
    'pubsub',
], function(
    uiFilter,
    uiBrowser,
    dbm,
    $,
    ps
){
//////////////////////////////////////////////////////////////////////////////

$(function(){
    ps.publish('evt:document.ready');
});

//////////////////////////////////////////////////////////////////////////////
});
