require([
    'ui.filter',
    'dbmanage',
    'jquery',
    'pubsub',
], function(
    uiFilter,
    dbm,
    $,
    ps
){
//////////////////////////////////////////////////////////////////////////////

$(function(){
    ps.publish('evt:document.ready');
});

//////////////////////////////////////////////////////////////////////////////
});
